spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:13030
server:
  port: 14040

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
debug: true